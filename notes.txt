git init 
#git创建

mkdir 
#创建文件夹

cd /xxx 
#进入某目录 绝对路径

cd xxx 
#进入某目录 相对路径

ls -ah/ls/dir
#查看文件包含隐藏文件/查看普通文件/查看普通文件

git add 
#添加文件到仓库，需在仓库目录下运行

git commit -m "xxx" 
#提交文件到仓库，需在仓库目录下运行,-m后面输入的是本次提交的说明
commit命令可以一次提交很多文件，例：
$ git add file1.txt
$ git add file2.txt file3.txt
$ git commit -m "add 3 files."

git status
#查看仓库当前状态

git diff
#查看修改内容/difference(差异)